VITE_DATA_MODE=http
VITE_API_BASE_URL=http://localhost:8000
# Sentiment is its own service (src/sentiment) on a separate origin, because
# VITE_API_BASE_URL above is the Python BIPs API and cannot serve both.
VITE_SENTIMENT_BASE_URL=http://localhost:8002
